<h2>User info</h2>
    <div>

      　<%= image_tag user.get_profile_image(100,100) %>
      <table class="table">
        <tbody>
          <tr>
            <th>name</th>
            <th><%= user.name %></th>
          </tr>
          <tr>
            <th>introduction</th>
            <th><%= user.introduction %></th>
          </tr>
        </tbody>
      </table>
      </div>

      <div class="row">
           <%= link_to edit_user_path(user.id) do %>
              <button type="button" class="btn btn-outline-secondary btn-block link-secondary"><i class="fas fa-user-cog"></i>
           <% end %>
      </div>